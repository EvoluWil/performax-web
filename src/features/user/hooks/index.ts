import {
  useUserRoleMutation,
  useUserRolesQuery,
} from "./queries/user-role.query";
import { useUserMutation, useUsersQuery } from "./queries/user.query";

export {
  useUserMutation,
  useUserRoleMutation,
  useUserRolesQuery,
  useUsersQuery,
};
